/*writes a word backwards*/
#include <stdio.h>
float square_root(float x){
float i=1,guess=1;
const float epilson=0.00001;
guess =;
return guess;
}
int main(void){
float n,k;
printf("input the number to find its square root:");
scanf("%f",&n);
k = square_root(n);
printf("the square root is %.2f\n",k);
}
